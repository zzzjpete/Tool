curl_cffi>=0.7.0
aiosqlite>=0.19.0
PyYAML>=6.0
click>=8.1.0
tenacity>=8.2.0
loguru>=0.7.0
pydantic>=2.5.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
fake-useragent>=1.4.0
pandas>=2.2.0
matplotlib>=3.8.0

# Optional — only needed for the BrowserSession fallback (scraper/core/browser.py).
# Install with: pip install playwright && python -m playwright install chromium
# playwright>=1.45.0

# Optional — recommended alongside playwright. Layers ~20 extra fingerprint
# defenses on top of BrowserSession's manual stealth (canvas/WebGL/AudioContext/
# mediaDevices/permissions/screen). Without it, sites like Zhihu's anti-bot
# may still pin the browser as automated. Auto-detected at runtime — install
# enables, missing is silently skipped.
# playwright-stealth>=2.0.0
